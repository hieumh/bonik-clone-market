import { Box } from "@mui/material";
import { FC } from "react";

interface INotificationIconWrapper {}

const NotificationIconWrapper: FC<INotificationIconWrapper> = () => {
  return <Box></Box>;
};

export default NotificationIconWrapper;
